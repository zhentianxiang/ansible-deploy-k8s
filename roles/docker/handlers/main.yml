- name: reload systemd
  become: yes
  command: systemctl daemon-reexec

- name: restart docker
  become: yes
  systemd:
    name: "{{ item }}"
    state: restarted
    enabled: yes
  loop:
    - docker
